import { HighlightCourseDirective } from './highlight-course.directive';

describe('HighlightCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCourseDirective();
    expect(directive).toBeTruthy();
  });
});
